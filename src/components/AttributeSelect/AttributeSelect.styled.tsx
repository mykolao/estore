import styled from 'styled-components';

export const AttributeSelectStyled = styled.div`
  padding-top: 24px;

  .label {
    font-family: 'Roboto Condensed', 'sans-serif';
    font-weight: 700;
    font-size: 18px;
    line-height: 18px;
    text-transform: uppercase;
  }

  .select {
    margin-top: 8px;
    display: flex;
    gap: 8px;
  }
`;
